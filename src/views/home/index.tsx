import { Button } from 'antd';
import Menu from './Menu.tsx';
const Home = () => {
  return (
    <div>
      <Button type="primary">按钮</Button>
      <Menu />
    </div>
  );
};

export default Home;
